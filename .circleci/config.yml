# This config was automatically generated from your source code
# Stacks detected: deps:node:visual studio code stuff for SECURITY/backend
version: 2.1
orbs:
  node: circleci/node@5
jobs:
  test-node:
    # Install node dependencies and run tests
    executor: node/default
    working_directory: ~/project/visual studio code stuff for SECURITY/backend
    steps:
      - checkout:
          path: ~/project
      - node/install-packages:
          pkg-manager: npm
      - run:
          name: Run tests
          command: echo \"No test specified in package.json\"
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'


          sonarqube:
  working_directory: ~/build
  docker:
    - image: sonarsource/sonar-scanner-cli
  resource_class: small
  steps:
    - run: find .
    - attach_workspace:
        at: ~/workspace
    - run:
          SONAR_BRANCH="${CIRCLE_BRANCH:-master}"
          SONAR_ORG="your-organization-key"
          SONAR_PROJECT_KEY="your-project-key"
          SONAR_HOST_URL="https://sonarcloud.io"
          SONAR_TOKEN="your-sonarqube-token"
          
          echo "Sonar branch value is: $SONAR_BRANCH"
          echo "Sonar org value is: $SONAR_ORG"
          
          sonar-scanner \
            -Dsonar.organization=$SONAR_ORG \
            -Dsonar.projectKey=$SONAR_PROJECT_KEY \
            -Dsonar.sources=. \
            -Dsonar.host.url=$SONAR_HOST_URL \
            -Dsonar.login=$SONAR_TOKEN \
            -Dsonar.branch.name=$SONAR_BRANCH


          
          
workflows:
  build-and-test:
    jobs:
      - test-node
      - sonarqube
    # - deploy:
    #     requires:
    #       - test-node
